server {
	listen 80 default_server;
	root /usr/share/nginx/default;
	index index.html;
	autoindex off;
	
	# Search Engine Friendly URLs
	location / {
		proxy_pass http://%SYSTEM_PANEL%.local/;
		include proxy_params;
	}
}